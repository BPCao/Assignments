function caesar(oldString, shifter)
{
    oldString.toLowerCase()
    let cipher = shifter > 26 ? shifter % 26 : shifter
    oldString.split()
    newString = oldString.map(char =>
        {
            
        }) 
    
}

console.log('ABCD')